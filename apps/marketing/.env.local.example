# App
NEXT_PUBLIC_APP_URL=http://localhost:3001
# Cross-domain link target for Login/Signup CTAs (marketing-navbar.tsx, lib/app-url.ts).
# In production this is the dashboard app's domain, e.g. app.pinmarks.in.
NEXT_PUBLIC_APP_HOST=

# Contact form (Resend)
RESEND_API_KEY=re_your_key
